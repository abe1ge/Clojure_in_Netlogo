(ns clojure-in-netlogo.core
  (:gen-class)
  (require [comm.nexec :refer :all]
           [comm.wrapper :refer :all])
  )

(defn -main
  "I don't do a whole lot ... yet."
  [& args]
  (println "Hello, World!"))
